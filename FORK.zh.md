# 关于此 Fork

[English](FORK.md)

> [!WARNING]
> 此 fork 中的大部分修改由 LLM agent 协助设计和实现。虽然开发过程中对部分修改
> 进行了针对性的构建和测试，但尚未完整测试整个 fork。请预期其中仍可能存在 bug
> 和功能退化；依赖这些修改之前请先审查代码，并谨慎使用。
>
> 最坏情况下，该版本可能会崩溃，并且你打开的内容会全部丢失。
>
> 考虑到 fork 的性质，这些修改几乎不可能被上游合并。你可以任意使用这些代码。

## 选中即复制支持

一个习惯了但上游[暂时不想实现](https://gitlab.gnome.org/chergert/ptyxis/-/work_items/276)的特性。

## 可选的 fish shell integration

Ptyxis 依靠 VTE shell integration 判断交互式命令何时开始和结束。VTE 目前为 bash 和
zsh 安装了相应集成，但没有覆盖 fish；因此 fish 会话默认不会触发 Ptyxis 的命令
完成通知。

此 fork 在 [`contrib/fish/ptyxis-vte.fish`](contrib/fish/ptyxis-vte.fish)
提供了一个可选脚本，但 Ptyxis 不会安装它。若要为当前用户启用，可以将其复制到
fish 配置目录：

```sh
mkdir -p ~/.config/fish/conf.d
cp contrib/fish/ptyxis-vte.fish ~/.config/fish/conf.d/
```

若仓库检出路径长期不变，也可以创建软链接：

```sh
mkdir -p ~/.config/fish/conf.d
ln -s "$(pwd)/contrib/fish/ptyxis-vte.fish" ~/.config/fish/conf.d/ptyxis-vte.fish
```

脚本仅在 VTE 0.78 或更新版本下的交互式 fish 中启用。它使用 fish 的
`fish_preexec` 和 `fish_postexec` 事件，不会替换或包装用户的 prompt。在容器或
远程主机中启动 fish 时，需要在相应环境中另行放置该脚本。

## 后台 bell 通知

当终端 bell 来自后台标签页或 pane，或其 Ptyxis 窗口处于非活动状态时，此 fork 除了
把标签页标记为需要注意，还会发送桌面通知。同一 pane 重复发出的 bell 会更新已有
通知而不会不断堆叠。激活通知会回到来源窗口、标签页和准确的 pane；聚焦或关闭该
pane 时会撤回通知。

活动窗口中当前可见 pane 发出的 bell 保持原有声音和视觉反馈，不会额外创建重复的
桌面通知。

## 终端 pane 与分屏

此 fork 最大的修改是在一个标签页中支持多个终端 pane。

- 新增 `PtyxisPane` 抽象，接管此前属于整个标签页的状态，包括终端控件、配置档、
  缩放、子进程、进程监控、OSC 标题策略、通知以及启动和会话状态。
- 使用分屏树模型表示嵌套的横向和纵向布局。可以自动或按指定方向切割 pane、按方向
  移动焦点，以及单独关闭某个 pane。
- 新分屏的分隔线默认居中。关闭 pane 时会先移动焦点，再折叠分屏树；只要仍有其他
  pane，就不会关闭标签页。
- 终端设置和配置档修改会应用到正确的 pane，而非只应用到当时恰好处于活动状态的
  pane。
- 标签页级兼容属性跟随活动 pane；后台 pane 的进程事件、通知、agent 更新和配置档
  生命周期则继续与其来源 pane 关联。
- 关闭确认会针对相应 pane；运行中进程对话框则会在适当情况下列出所有 pane 中的
  进程。
- 分屏区域会暴露给无障碍工具；从搜索结果定位 pane 时，会在视觉上突出显示目标
  pane。

默认且可配置的快捷键如下（类似 Tilix）：

| 操作 | 默认快捷键 |
| --- | --- |
| 自动切割 (**A**uto) | `Ctrl+Alt+A` |
| 横向切割（上下布局, **D**own） | `Ctrl+Alt+D` |
| 纵向切割（左右布局, **R**ight） | `Ctrl+Alt+R` |
| 向左／右／上／下聚焦 pane | `Alt+方向键` |

终端右键菜单包含全部三种切割操作，并显示其当前配置的快捷键。按方向移动 pane
焦点的快捷键可在首选项中配置。

## 窗口尺寸与会话恢复

- 会话格式版本 2 会序列化完整的嵌套分屏布局、pane 标识、活动 pane、分隔线比例，
  以及完整的终端字符网格尺寸。
- 恢复会话时会重建分屏树和窗口尺寸，而不是把 pane 展平为多个标签页。
- 窗口尺寸计算会忽略分屏标签页中各 pane 的合计字符网格。创建分屏、切回分屏
  标签页，或从分屏布局中新建标签页时，窗口不应再意外缩小到终端最小尺寸（通常为
  80x24）。
- pane 标识在焦点变化时保持稳定，因此延迟回调和恢复的状态仍能找到正确的 pane。

新的会话表示是下游格式扩展。它与其他 Ptyxis 构建之间的兼容性尚未经过全面测试，
尤其是在不同构建之间来回迁移已保存会话的情况。

## 搜索与终端导航

- 现有标签页概览会搜索并显示一个分屏标签页中所有 pane 的标题，而不再仅处理活动
  pane。
- `Ctrl+Shift+P` 会打开跨窗口的“Go to Terminal”选择器。它搜索当前应用进程内
  所有 Ptyxis 窗口中的全部 pane，使用不区分大小写的多词模糊匹配，并在排序时考虑
  最近使用的终端。
- 激活结果会选择对应窗口、标签页和确切的 pane，然后把键盘焦点交给该终端。
- 选择器支持键盘和鼠标激活；按 `Escape` 可关闭。
- 此快捷键可在首选项中配置。原有的 `Ctrl+Shift+O` 标签页概览仍然保留，并未被
  替换。

## Quake 风格窗口

- `ptyxis --toggle-quake` 用于显示或隐藏一个专用且持久的终端窗口。重复执行此命令
  会复用该窗口，而不是创建普通窗口。
- Quake 窗口不会参与普通的最近使用窗口选择、常规尺寸决策或会话持久化。
- 标题栏和样式会明确标识它是 Quake 终端，以便与普通 Ptyxis 窗口区分。
- 一个很小的 `ptyxis-quake-daemon` 进程负责持有 Global Shortcuts portal 会话，
  首选按键为 `Ctrl+grave`（Ctrl 加反引号键）。它只会在首次使用 Quake 模式或在
  首选项中明确启用后启动，并不包含终端或窗口实现。
- 首次使用时，Ptyxis 会询问是否在登录时启动快捷键服务。Flatpak 构建通过
  Background portal 注册，原生构建则使用当前用户的 XDG autostart 项。选择不
  自启动时，快捷键在当前登录会话中仍然可用；之后可在首选项的“行为”页面修改。
- 已有的 portal 绑定会静默恢复；若尚无绑定，daemon 会请求创建绑定，此时桌面
  环境可能显示许可对话框。
- 首选项提供“更改快捷键…”按钮，通过 Global Shortcuts portal v2 打开由桌面环境
  管理的可信快捷键配置界面。
  同一区域也可以启动或停止当前登录会话中的 daemon；daemon 停止时，依赖它的
  控件会被禁用。此操作不会改变独立的登录时自启动选项。
- 从 portal 激活快捷键时会使用合成器提供的 activation token。Quake 窗口隐藏或
  可见但未聚焦时，快捷键会将它带到前台；窗口已聚焦时则会隐藏它。不带
  直接执行 `ptyxis --toggle-quake` 时，仍保持原有的显示／隐藏切换行为。

窗口位置和动画仍由合成器负责。尤其是 Wayland 协议不允许普通应用可靠地指定屏幕
边缘的绝对位置，因此无法保证它在所有桌面上都能像合成器原生的下拉终端一样工作。

## 调色盘

- 移除了单独内置的 `Solarized Light` 调色盘。
- 保留的 Solarized 调色盘会跟随系统明暗偏好，并采用参考 Ghostty 的 ANSI 颜色，
  提高 `htop` 等终端程序中的对比度。

这会改变现有用户使用内置 Solarized 调色盘时看到的效果。针对已报告的可见性问题
进行过检查，但尚未穷尽各种终端程序中的显示情况。

## 发行版打包

此 fork 新增了以下本地打包流程：

- Debian sid 和 Ubuntu 26.04（`.deb`）
- Fedora 44（`.rpm`）
- Arch Linux（`.pkg.tar.zst`）
- 使用 GNOME 50 runtime 的 Flatpak（`.flatpak`）

`packaging/build-packages.sh` 使用 Docker 或 Podman 构建原生包，并根据 Git 修订生成
可复现的快照版本。若工作区中存在已跟踪文件的修改，脚本会拒绝打包；构建源代码
来自 `git archive HEAD`。Flatpak 临时数据保存在 `dist/` 下，以确保 OSTree 仓库和
构建目录位于同一文件系统。

目标要求、构建命令、产物位置、安装说明和包级验证清单参见
[docs/PACKAGING.md](docs/PACKAGING.md)。生成的软件包没有签名，并使用 Ptyxis 的正常
软件包名称和应用 ID，因此安装后可能替换发行版提供的构建。

## 测试覆盖与剩余风险

此分支包含分屏树模型和终端选择器匹配逻辑的单元测试。开发过程中还运行过针对性的
Meson 构建和部分软件包构建。这些检查并不代表整个 fork 已得到完整验证。

以下方面尤其需要更广泛的人工测试：

- 深层嵌套分屏的创建、关闭、焦点和尺寸调整行为；
- 异常布局和失败场景下的会话迁移与恢复；
- 后台 pane 中的子进程、通知、配置档和容器行为；
- 配合多种辅助技术的无障碍行为；
- 不同 Wayland 合成器和 X11 窗口管理器下的 Quake 窗口；
- 不同 GTK/libadwaita 版本下的键盘焦点和对话框生命周期；
- 每种打包目标的安装、升级和运行时集成；
- 不同程序、主题和显示配置下的颜色可读性。

构建或单元测试能够通过，并不表示所有 GUI、会话、合成器或打包路径都已达到可用于
生产环境的程度。
